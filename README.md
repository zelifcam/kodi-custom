
# PKGBUILD Arch Linux
## kodi-custom
```
./build.sh --help
Usage: ./build.sh [--rebuild] [kodi|addons|all]
  kodi      - build kodi-gbm-git package
  addons    - build kodi-gbm-addons-git package
  all       - build both (default)
  --rebuild - force rebuild of the docker image
```
